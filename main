<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>VertexTW — Hall Effect Gaming Keyboards</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<style>
:root {
  --green: #00e5a0;
  --green-dim: rgba(0,229,160,0.12);
  --green-glow: rgba(0,229,160,0.22);
  --bg: #060810;
  --surface: #0d1018;
  --surface2: #131720;
  --border: rgba(255,255,255,0.07);
  --border-hover: rgba(0,229,160,0.35);
  --text: #e8edf5;
  --muted: #7a8496;
  --danger: #ff4f4f;
}

* { margin:0; padding:0; box-sizing:border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow-x: hidden;
  min-height: 100vh;
}

/* ---- PAGES ---- */
.page { display: none; min-height: 100vh; }
.page.active { display: block; }

/* ---- NAV ---- */
nav {
  position: fixed; top: 0; left: 0; right: 0; z-index: 900;
  background: rgba(6,8,16,0.85);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid var(--border);
  padding: 0 5%;
  height: 64px;
  display: flex; align-items: center; justify-content: space-between;
}

.logo {
  font-family: 'Syne', sans-serif;
  font-size: 1.5rem; font-weight: 800;
  color: var(--text);
  cursor: pointer;
}
.logo span { color: var(--green); }

.nav-links {
  display: flex; align-items: center; gap: 32px;
  list-style: none;
}
.nav-links a {
  font-size: 0.9rem; color: var(--muted);
  text-decoration: none; transition: color 0.2s;
  cursor: pointer;
}
.nav-links a:hover { color: var(--green); }

.nav-right { display: flex; align-items: center; gap: 16px; }

.cart-btn-nav {
  background: var(--green-dim);
  border: 1px solid var(--border-hover);
  color: var(--green);
  padding: 8px 18px;
  border-radius: 50px;
  cursor: pointer;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.88rem; font-weight: 500;
  display: flex; align-items: center; gap: 8px;
  transition: all 0.2s;
}
.cart-btn-nav:hover { background: rgba(0,229,160,0.2); transform: translateY(-1px); }

.cart-count {
  background: var(--green);
  color: var(--bg);
  width: 20px; height: 20px;
  border-radius: 50%;
  font-size: 0.72rem; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
}

/* ---- HERO / SLIDESHOW ---- */
.hero {
  min-height: 100vh;
  display: flex; flex-direction: column;
  padding-top: 64px;
  position: relative; overflow: hidden;
}

.hero-bg {
  position: absolute; inset: 0; z-index: 0;
  overflow: hidden;
}
.hero-bg::before {
  content: '';
  position: absolute;
  width: 800px; height: 800px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(0,229,160,0.08) 0%, transparent 70%);
  top: -200px; right: -200px;
  animation: pulse 8s ease-in-out infinite;
}
.hero-bg::after {
  content: '';
  position: absolute;
  width: 600px; height: 600px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(70,100,255,0.06) 0%, transparent 70%);
  bottom: -100px; left: -100px;
  animation: pulse 10s ease-in-out infinite 2s;
}
@keyframes pulse {
  0%,100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.1); opacity: 0.7; }
}

/* Slideshow */
.slideshow {
  position: relative; z-index: 1;
  flex: 1;
  display: flex; align-items: center;
  padding: 60px 5%;
}

.slides-wrapper {
  position: relative; width: 100%;
  min-height: 520px;
}

.slide {
  position: absolute; inset: 0;
  display: flex; align-items: center;
  gap: 80px;
  opacity: 0;
  transform: translateX(60px);
  transition: all 0.6s cubic-bezier(0.25,0.46,0.45,0.94);
  pointer-events: none;
}
.slide.active {
  opacity: 1;
  transform: translateX(0);
  pointer-events: auto;
}
.slide.exit-left {
  opacity: 0;
  transform: translateX(-60px);
}

.slide-visual {
  flex: 1; min-width: 0;
  display: flex; align-items: center; justify-content: center;
}

.keyboard-display {
  width: 100%; max-width: 500px;
  height: 300px;
  border-radius: 20px;
  border: 1px solid var(--border);
  background: var(--surface);
  display: flex; align-items: center; justify-content: center;
  position: relative;
  overflow: hidden;
}

.keyboard-display::before {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(135deg, rgba(0,229,160,0.04) 0%, transparent 60%);
}

.keyboard-emoji {
  font-size: 6rem;
  filter: drop-shadow(0 0 40px rgba(0,229,160,0.3));
  animation: float 4s ease-in-out infinite;
}
@keyframes float {
  0%,100% { transform: translateY(0); }
  50% { transform: translateY(-12px); }
}

.kb-label {
  position: absolute; bottom: 16px; left: 20px;
  font-size: 0.75rem; color: var(--muted); letter-spacing: 0.05em;
}

.slide-info { flex: 1; min-width: 0; }

.slide-badge {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 6px 14px;
  border-radius: 50px;
  border: 1px solid var(--border-hover);
  background: var(--green-dim);
  color: var(--green);
  font-size: 0.8rem; font-weight: 500;
  margin-bottom: 24px;
}

.slide-info h1 {
  font-family: 'Syne', sans-serif;
  font-size: clamp(2.4rem, 5vw, 4rem);
  font-weight: 800;
  line-height: 1.05;
  margin-bottom: 20px;
}
.slide-info h1 em {
  font-style: normal;
  color: var(--green);
}

.slide-info p {
  color: var(--muted);
  font-size: 1.05rem;
  line-height: 1.8;
  margin-bottom: 32px;
  max-width: 420px;
}

.slide-price-row {
  display: flex; align-items: center; gap: 20px;
  margin-bottom: 32px;
}

.slide-price {
  font-family: 'Syne', sans-serif;
  font-size: 2.4rem; font-weight: 700;
  color: var(--green);
}

.price-note {
  font-size: 0.82rem; color: var(--muted);
  line-height: 1.4;
}

.slide-actions { display: flex; gap: 14px; flex-wrap: wrap; }

.btn-primary {
  background: var(--green);
  color: var(--bg);
  border: none;
  padding: 14px 28px;
  border-radius: 12px;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.95rem; font-weight: 600;
  cursor: pointer;
  transition: all 0.25s;
  display: flex; align-items: center; gap: 8px;
}
.btn-primary:hover { transform: translateY(-3px); box-shadow: 0 12px 32px var(--green-glow); }

.btn-ghost {
  background: var(--surface2);
  color: var(--text);
  border: 1px solid var(--border);
  padding: 14px 28px;
  border-radius: 12px;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.95rem; font-weight: 500;
  cursor: pointer;
  transition: all 0.25s;
}
.btn-ghost:hover { border-color: var(--border-hover); color: var(--green); }

/* Slide controls */
.slide-controls {
  position: relative; z-index: 1;
  display: flex; align-items: center; justify-content: center;
  gap: 16px;
  padding: 0 5% 50px;
}

.dot-btn {
  width: 10px; height: 10px;
  border-radius: 50%;
  background: var(--border);
  border: none; cursor: pointer;
  transition: all 0.3s;
  padding: 0;
}
.dot-btn.active {
  background: var(--green);
  width: 28px;
  border-radius: 5px;
}

.arrow-btn {
  background: var(--surface2);
  border: 1px solid var(--border);
  color: var(--muted);
  width: 40px; height: 40px;
  border-radius: 50%;
  cursor: pointer;
  font-size: 1rem;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s;
}
.arrow-btn:hover { border-color: var(--border-hover); color: var(--green); }

/* ---- PRODUCTS SECTION ---- */
.products-section {
  padding: 80px 5%;
  background: var(--surface);
  border-top: 1px solid var(--border);
}

.section-header {
  display: flex; align-items: flex-end; justify-content: space-between;
  margin-bottom: 48px;
}

.section-header h2 {
  font-family: 'Syne', sans-serif;
  font-size: 2.4rem; font-weight: 700;
}
.section-header h2 span { color: var(--green); }

.see-all {
  color: var(--green); font-size: 0.9rem;
  text-decoration: none; cursor: pointer;
  display: flex; align-items: center; gap: 6px;
  transition: gap 0.2s;
}
.see-all:hover { gap: 10px; }

.products-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 24px;
}

.product-card {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 20px;
  overflow: hidden;
  cursor: pointer;
  transition: all 0.35s;
  position: relative;
}
.product-card:hover {
  transform: translateY(-8px);
  border-color: var(--border-hover);
  box-shadow: 0 24px 64px rgba(0,0,0,0.5), 0 0 0 1px var(--border-hover);
}

.product-card-img {
  width: 100%; height: 200px;
  background: var(--surface2);
  display: flex; align-items: center; justify-content: center;
  font-size: 4.5rem;
  position: relative;
  overflow: hidden;
  border-bottom: 1px solid var(--border);
}
.product-card-img::after {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(180deg, transparent 40%, rgba(6,8,16,0.3) 100%);
}

.product-card-body { padding: 22px; }

.product-card .badge {
  display: inline-block;
  padding: 4px 12px;
  border-radius: 50px;
  background: var(--green-dim);
  border: 1px solid var(--border-hover);
  color: var(--green);
  font-size: 0.75rem; font-weight: 500;
  margin-bottom: 12px;
}

.product-card h3 {
  font-family: 'Syne', sans-serif;
  font-size: 1.35rem; font-weight: 700;
  margin-bottom: 8px;
}

.product-card .desc {
  color: var(--muted); font-size: 0.88rem;
  line-height: 1.7; margin-bottom: 18px;
}

.product-card-footer {
  display: flex; align-items: center; justify-content: space-between;
}

.product-price {
  font-family: 'Syne', sans-serif;
  font-size: 1.6rem; font-weight: 700;
  color: var(--green);
}

.add-cart-mini {
  background: var(--green-dim);
  border: 1px solid var(--border-hover);
  color: var(--green);
  padding: 8px 16px;
  border-radius: 10px;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.82rem; font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}
.add-cart-mini:hover { background: rgba(0,229,160,0.22); }

/* ---- PRODUCT DETAIL PAGE ---- */
.product-detail {
  padding: 100px 5% 80px;
  max-width: 1200px;
  margin: 0 auto;
}

.back-link {
  display: inline-flex; align-items: center; gap: 8px;
  color: var(--muted); font-size: 0.9rem;
  cursor: pointer; margin-bottom: 48px;
  transition: color 0.2s;
  background: none; border: none;
  font-family: 'DM Sans', sans-serif;
}
.back-link:hover { color: var(--green); }

.detail-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 72px;
  align-items: start;
}

.detail-visual {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 24px;
  height: 400px;
  display: flex; align-items: center; justify-content: center;
  font-size: 8rem;
  position: sticky; top: 84px;
  overflow: hidden;
}
.detail-visual::before {
  content: '';
  position: absolute; inset: 0;
  background: radial-gradient(circle at 50% 50%, rgba(0,229,160,0.08), transparent 70%);
}

.detail-info .brand {
  font-size: 0.8rem; color: var(--green);
  letter-spacing: 0.12em; text-transform: uppercase;
  margin-bottom: 12px;
}

.detail-info h1 {
  font-family: 'Syne', sans-serif;
  font-size: 3rem; font-weight: 800;
  margin-bottom: 16px;
}

.detail-badges {
  display: flex; gap: 10px; flex-wrap: wrap;
  margin-bottom: 28px;
}

.detail-badge {
  padding: 6px 14px;
  border-radius: 50px;
  background: var(--surface2);
  border: 1px solid var(--border);
  color: var(--muted);
  font-size: 0.8rem;
}

.detail-desc {
  color: var(--muted);
  line-height: 1.9;
  font-size: 1rem;
  margin-bottom: 36px;
}

.specs-grid {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 12px; margin-bottom: 36px;
}

.spec-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 14px 16px;
}
.spec-item .spec-label {
  font-size: 0.75rem; color: var(--muted);
  margin-bottom: 4px; text-transform: uppercase;
  letter-spacing: 0.06em;
}
.spec-item .spec-val {
  font-size: 0.95rem; font-weight: 500;
}

.detail-price-row {
  display: flex; align-items: baseline; gap: 16px;
  margin-bottom: 28px;
}
.detail-price {
  font-family: 'Syne', sans-serif;
  font-size: 2.8rem; font-weight: 800;
  color: var(--green);
}
.detail-price-note { font-size: 0.85rem; color: var(--muted); }

/* Color / switch selector */
.color-section { margin-bottom: 28px; }
.color-section label {
  font-size: 0.85rem; color: var(--muted);
  display: block; margin-bottom: 10px;
}
.color-options { display: flex; gap: 10px; }
.color-swatch {
  width: 28px; height: 28px; border-radius: 50%;
  border: 2px solid transparent;
  cursor: pointer; transition: all 0.2s;
}
.color-swatch.selected { border-color: var(--green); box-shadow: 0 0 0 3px var(--green-dim); }

.switch-section { margin-bottom: 32px; }
.switch-section label {
  font-size: 0.85rem; color: var(--muted);
  display: block; margin-bottom: 10px;
}
.switch-options { display: flex; gap: 10px; }
.switch-opt {
  padding: 8px 18px;
  border-radius: 10px;
  border: 1px solid var(--border);
  background: none;
  color: var(--muted);
  font-family: 'DM Sans', sans-serif;
  font-size: 0.85rem;
  cursor: pointer;
  transition: all 0.2s;
}
.switch-opt:hover { border-color: var(--border-hover); color: var(--text); }
.switch-opt.selected { border-color: var(--green); background: var(--green-dim); color: var(--green); }

.detail-actions { display: flex; gap: 14px; }

.btn-buy-now {
  flex: 1;
  background: var(--green);
  color: var(--bg);
  border: none;
  padding: 16px 0;
  border-radius: 14px;
  font-family: 'DM Sans', sans-serif;
  font-size: 1rem; font-weight: 700;
  cursor: pointer;
  transition: all 0.25s;
}
.btn-buy-now:hover { transform: translateY(-3px); box-shadow: 0 16px 40px var(--green-glow); }

.btn-add-cart-large {
  flex: 1;
  background: var(--surface2);
  color: var(--text);
  border: 1px solid var(--border);
  padding: 16px 0;
  border-radius: 14px;
  font-family: 'DM Sans', sans-serif;
  font-size: 1rem; font-weight: 600;
  cursor: pointer;
  transition: all 0.25s;
}
.btn-add-cart-large:hover { border-color: var(--border-hover); color: var(--green); }

/* ---- CART DRAWER ---- */
.cart-overlay {
  position: fixed; inset: 0; z-index: 1000;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(4px);
  opacity: 0; pointer-events: none;
  transition: opacity 0.3s;
}
.cart-overlay.open { opacity: 1; pointer-events: auto; }

.cart-drawer {
  position: fixed; top: 0; right: 0; bottom: 0; z-index: 1001;
  width: 420px; max-width: 95vw;
  background: var(--surface);
  border-left: 1px solid var(--border);
  display: flex; flex-direction: column;
  transform: translateX(100%);
  transition: transform 0.4s cubic-bezier(0.25,0.46,0.45,0.94);
}
.cart-drawer.open { transform: translateX(0); }

.cart-header {
  display: flex; align-items: center; justify-content: space-between;
  padding: 24px 24px 20px;
  border-bottom: 1px solid var(--border);
}
.cart-header h2 {
  font-family: 'Syne', sans-serif;
  font-size: 1.3rem; font-weight: 700;
}

.close-btn {
  width: 36px; height: 36px;
  background: var(--surface2);
  border: 1px solid var(--border);
  color: var(--muted);
  border-radius: 50%;
  cursor: pointer;
  font-size: 1.1rem;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s;
}
.close-btn:hover { border-color: var(--danger); color: var(--danger); }

.cart-body {
  flex: 1; overflow-y: auto;
  padding: 20px 24px;
}

.cart-empty {
  text-align: center;
  padding: 60px 0;
  color: var(--muted);
}
.cart-empty-icon { font-size: 4rem; margin-bottom: 16px; opacity: 0.5; }
.cart-empty p { font-size: 1rem; }

.cart-item-row {
  display: flex; align-items: center; gap: 14px;
  padding: 16px 0;
  border-bottom: 1px solid var(--border);
}

.cart-item-img {
  width: 60px; height: 60px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.8rem;
  flex-shrink: 0;
}

.cart-item-info { flex: 1; min-width: 0; }
.cart-item-name {
  font-weight: 500; font-size: 0.95rem;
  margin-bottom: 4px;
  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
}
.cart-item-meta { font-size: 0.8rem; color: var(--muted); }

.cart-item-right {
  display: flex; align-items: center; gap: 12px;
  flex-shrink: 0;
}

.qty-control {
  display: flex; align-items: center; gap: 8px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 4px 8px;
}
.qty-btn {
  background: none; border: none;
  color: var(--muted); cursor: pointer;
  width: 22px; height: 22px;
  font-size: 1rem;
  display: flex; align-items: center; justify-content: center;
  border-radius: 4px;
  transition: all 0.15s;
}
.qty-btn:hover { background: var(--surface2); color: var(--green); }
.qty-num { font-size: 0.9rem; font-weight: 500; min-width: 16px; text-align: center; }

.cart-item-price {
  font-family: 'Syne', sans-serif;
  font-size: 1rem; font-weight: 600;
  color: var(--green);
  min-width: 72px; text-align: right;
}

.remove-item {
  background: none; border: none;
  color: var(--muted); cursor: pointer;
  font-size: 1rem;
  width: 28px; height: 28px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 50%;
  transition: all 0.15s;
}
.remove-item:hover { background: rgba(255,79,79,0.1); color: var(--danger); }

.cart-footer {
  padding: 20px 24px;
  border-top: 1px solid var(--border);
}

.cart-summary {
  margin-bottom: 18px;
}
.cart-summary-row {
  display: flex; justify-content: space-between;
  font-size: 0.9rem; color: var(--muted);
  margin-bottom: 8px;
}
.cart-summary-row.total {
  font-size: 1.1rem; color: var(--text);
  font-weight: 600; margin-top: 12px;
  padding-top: 12px;
  border-top: 1px solid var(--border);
}
.cart-summary-row.total .total-amount {
  font-family: 'Syne', sans-serif;
  color: var(--green); font-size: 1.3rem;
}

.btn-checkout {
  width: 100%;
  background: var(--green);
  color: var(--bg);
  border: none;
  padding: 16px;
  border-radius: 14px;
  font-family: 'DM Sans', sans-serif;
  font-size: 1rem; font-weight: 700;
  cursor: pointer;
  transition: all 0.25s;
  margin-bottom: 10px;
}
.btn-checkout:hover { transform: translateY(-2px); box-shadow: 0 12px 32px var(--green-glow); }
.btn-checkout:disabled {
  opacity: 0.4; cursor: not-allowed; transform: none; box-shadow: none;
}

.btn-continue {
  width: 100%;
  background: none;
  color: var(--muted);
  border: none;
  padding: 10px;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.88rem;
  cursor: pointer;
  transition: color 0.2s;
}
.btn-continue:hover { color: var(--green); }

/* ---- CHECKOUT PAGE ---- */
.checkout-page {
  padding: 100px 5% 80px;
  max-width: 1100px;
  margin: 0 auto;
}

.checkout-page h1 {
  font-family: 'Syne', sans-serif;
  font-size: 2.4rem; font-weight: 800;
  margin-bottom: 8px;
}
.checkout-page .sub {
  color: var(--muted); font-size: 0.95rem;
  margin-bottom: 48px;
}

.checkout-grid {
  display: grid;
  grid-template-columns: 1fr 420px;
  gap: 40px;
  align-items: start;
}

/* Steps */
.steps {
  display: flex; gap: 0;
  margin-bottom: 40px;
}
.step {
  display: flex; align-items: center; gap: 10px;
  font-size: 0.85rem; color: var(--muted);
}
.step.active { color: var(--text); }
.step.done { color: var(--green); }
.step-num {
  width: 28px; height: 28px;
  border-radius: 50%;
  background: var(--surface2);
  border: 1px solid var(--border);
  display: flex; align-items: center; justify-content: center;
  font-size: 0.78rem; font-weight: 600;
}
.step.active .step-num {
  background: var(--green); color: var(--bg); border-color: var(--green);
}
.step.done .step-num {
  background: var(--green-dim); color: var(--green); border-color: var(--border-hover);
}
.step-line {
  width: 40px; height: 1px;
  background: var(--border);
  margin: 0 4px;
}
.step-line.done { background: var(--green); }

/* Form */
.checkout-form { }

.form-section {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 28px;
  margin-bottom: 20px;
}
.form-section h3 {
  font-family: 'Syne', sans-serif;
  font-size: 1.1rem; font-weight: 700;
  margin-bottom: 22px;
  display: flex; align-items: center; gap: 10px;
}
.form-section h3 .icon {
  width: 32px; height: 32px;
  border-radius: 8px;
  background: var(--green-dim);
  border: 1px solid var(--border-hover);
  display: flex; align-items: center; justify-content: center;
  font-size: 0.95rem;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
  margin-bottom: 14px;
}
.form-row.full { grid-template-columns: 1fr; }

.form-group { display: flex; flex-direction: column; gap: 6px; }

.form-group label {
  font-size: 0.8rem; color: var(--muted);
  letter-spacing: 0.04em;
}

.form-group input, .form-group select {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 12px 14px;
  color: var(--text);
  font-family: 'DM Sans', sans-serif;
  font-size: 0.92rem;
  outline: none;
  transition: border-color 0.2s;
  -webkit-appearance: none;
}
.form-group input:focus, .form-group select:focus {
  border-color: var(--green);
  box-shadow: 0 0 0 3px var(--green-dim);
}
.form-group input::placeholder { color: #3a4050; }

/* Payment method tabs */
.payment-tabs {
  display: flex; gap: 10px; margin-bottom: 22px;
}
.pay-tab {
  flex: 1;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 12px;
  cursor: pointer;
  text-align: center;
  transition: all 0.2s;
  font-size: 0.85rem; color: var(--muted);
  font-family: 'DM Sans', sans-serif;
}
.pay-tab:hover { border-color: var(--border-hover); color: var(--text); }
.pay-tab.active { border-color: var(--green); background: var(--green-dim); color: var(--green); font-weight: 600; }

.pay-tab-icon { font-size: 1.4rem; margin-bottom: 4px; }

/* Card input visual */
.card-number-wrap {
  position: relative;
}
.card-number-wrap input { padding-right: 50px; letter-spacing: 0.1em; }
.card-brand {
  position: absolute; right: 14px; top: 50%; transform: translateY(-50%);
  font-size: 1.2rem;
}

/* Order summary sidebar */
.order-summary {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 28px;
  position: sticky; top: 84px;
}

.order-summary h3 {
  font-family: 'Syne', sans-serif;
  font-size: 1.1rem; font-weight: 700;
  margin-bottom: 22px;
  color: var(--text);
}

.order-items { margin-bottom: 20px; }
.order-item-row {
  display: flex; align-items: center; gap: 12px;
  padding: 10px 0;
  border-bottom: 1px solid var(--border);
}
.order-item-row:last-child { border-bottom: none; }
.order-item-emoji { font-size: 1.6rem; }
.order-item-info { flex: 1; min-width: 0; }
.order-item-name { font-size: 0.88rem; font-weight: 500; }
.order-item-qty { font-size: 0.78rem; color: var(--muted); margin-top: 2px; }
.order-item-price {
  font-family: 'Syne', sans-serif;
  font-size: 0.95rem; color: var(--green); font-weight: 600;
}

.order-totals { border-top: 1px solid var(--border); padding-top: 16px; }
.order-total-row {
  display: flex; justify-content: space-between;
  font-size: 0.88rem; color: var(--muted);
  margin-bottom: 10px;
}
.order-total-row.grand {
  font-size: 1.05rem;
  font-weight: 600; color: var(--text);
  margin-top: 12px; padding-top: 12px;
  border-top: 1px solid var(--border);
}
.order-total-row.grand .g-price {
  font-family: 'Syne', sans-serif;
  color: var(--green); font-size: 1.3rem;
}

.secure-badges {
  margin-top: 20px;
  display: flex; gap: 12px; justify-content: center;
}
.secure-badge {
  font-size: 0.72rem; color: var(--muted);
  display: flex; align-items: center; gap: 4px;
}

.btn-place-order {
  width: 100%;
  background: var(--green);
  color: var(--bg);
  border: none;
  padding: 17px;
  border-radius: 14px;
  font-family: 'DM Sans', sans-serif;
  font-size: 1.05rem; font-weight: 700;
  cursor: pointer;
  margin-top: 20px;
  transition: all 0.25s;
}
.btn-place-order:hover { transform: translateY(-2px); box-shadow: 0 12px 32px var(--green-glow); }

/* ---- SUCCESS PAGE ---- */
.success-page {
  min-height: 100vh;
  display: flex; align-items: center; justify-content: center;
  text-align: center;
  padding: 80px 5%;
}
.success-content { max-width: 520px; margin: 0 auto; }
.success-icon {
  width: 100px; height: 100px;
  border-radius: 50%;
  background: var(--green-dim);
  border: 2px solid var(--green);
  display: flex; align-items: center; justify-content: center;
  font-size: 2.5rem;
  margin: 0 auto 32px;
  animation: pop 0.5s cubic-bezier(0.17,0.89,0.32,1.28);
}
@keyframes pop {
  0% { transform: scale(0); }
  100% { transform: scale(1); }
}
.success-content h1 {
  font-family: 'Syne', sans-serif;
  font-size: 2.4rem; font-weight: 800;
  margin-bottom: 16px;
}
.success-content p { color: var(--muted); line-height: 1.8; margin-bottom: 36px; }
.order-num {
  display: inline-block;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 14px 28px;
  font-family: 'Syne', sans-serif;
  font-size: 1.1rem;
  margin-bottom: 36px;
  color: var(--green);
  letter-spacing: 0.08em;
}

/* ---- FOOTER ---- */
footer {
  padding: 40px 5%;
  border-top: 1px solid var(--border);
  display: flex; align-items: center; justify-content: space-between;
  margin-top: 60px;
}
.footer-logo { font-family: 'Syne', sans-serif; font-size: 1.2rem; font-weight: 700; }
.footer-logo span { color: var(--green); }
.footer-text { font-size: 0.82rem; color: var(--muted); }

/* ---- TOAST ---- */
.toast {
  position: fixed; bottom: 28px; left: 50%; transform: translateX(-50%) translateY(20px);
  background: var(--surface);
  border: 1px solid var(--border-hover);
  border-radius: 50px;
  padding: 12px 24px;
  font-size: 0.9rem;
  display: flex; align-items: center; gap: 10px;
  z-index: 2000;
  opacity: 0; pointer-events: none;
  transition: all 0.3s;
}
.toast.show {
  opacity: 1; pointer-events: auto;
  transform: translateX(-50%) translateY(0);
}
.toast-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--green); flex-shrink: 0; }

/* ---- RESPONSIVE ---- */
@media (max-width: 900px) {
  .slide { flex-direction: column; gap: 40px; }
  .slides-wrapper { min-height: 820px; }
  .detail-grid { grid-template-columns: 1fr; }
  .detail-visual { position: static; }
  .checkout-grid { grid-template-columns: 1fr; }
  .form-row { grid-template-columns: 1fr; }
  nav ul { display: none; }
  .order-summary { order: -1; }
}
@media (max-width: 600px) {
  .hero h1 { font-size: 2.2rem; }
  .slide-price { font-size: 1.8rem; }
  .products-grid { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo" onclick="showPage('home')">Vertex<span>TW</span></div>
  <ul class="nav-links">
    <li><a onclick="showPage('home')">首頁</a></li>
    <li><a onclick="scrollToProducts()">產品</a></li>
    <li><a>關於我們</a></li>
  </ul>
  <div class="nav-right">
    <button class="cart-btn-nav" onclick="toggleCart()">
      🛒 購物車
      <span class="cart-count" id="cart-count">0</span>
    </button>
  </div>
</nav>

<!-- CART OVERLAY -->
<div class="cart-overlay" id="cart-overlay" onclick="toggleCart()"></div>

<!-- CART DRAWER -->
<div class="cart-drawer" id="cart-drawer">
  <div class="cart-header">
    <h2>🛒 購物車</h2>
    <button class="close-btn" onclick="toggleCart()">✕</button>
  </div>
  <div class="cart-body" id="cart-body">
    <div class="cart-empty">
      <div class="cart-empty-icon">🛍️</div>
      <p>您的購物車是空的</p>
    </div>
  </div>
  <div class="cart-footer">
    <div class="cart-summary" id="cart-summary" style="display:none">
      <div class="cart-summary-row"><span>小計</span><span id="cart-subtotal">NT$0</span></div>
      <div class="cart-summary-row"><span>運費</span><span style="color:var(--green)">免費</span></div>
      <div class="cart-summary-row total"><span>總計</span><span class="total-amount" id="cart-total-display">NT$0</span></div>
    </div>
    <button class="btn-checkout" id="btn-checkout" onclick="goCheckout()" disabled>前往結帳</button>
    <button class="btn-continue" onclick="toggleCart()">繼續購物</button>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast">
  <div class="toast-dot"></div>
  <span id="toast-msg">已加入購物車！</span>
</div>

<!-- ===== HOME PAGE ===== -->
<div class="page active" id="page-home">

  <!-- HERO SLIDESHOW -->
  <section class="hero">
    <div class="hero-bg"></div>

    <div class="slideshow">
      <div class="slides-wrapper" id="slides-wrapper">

        <!-- SLIDE 1 -->
        <div class="slide active" data-idx="0">
          <div class="slide-visual">
            <div class="keyboard-display">
              <div class="keyboard-emoji">⌨️</div>
              <div class="kb-label">Nova HE60 · Carbon Black</div>
            </div>
          </div>
          <div class="slide-info">
            <div class="slide-badge">🔥 最熱銷款式</div>
            <h1>Nova<br><em>HE60</em></h1>
            <p>60% 緊湊配列，霍爾磁軸 Rapid Trigger 技術，
              每次擊鍵都帶來 FPS 專業玩家等級的反應速度。</p>
            <div class="slide-price-row">
              <div class="slide-price">NT$4,590</div>
              <div class="price-note">含稅・免運費<br>分期 0 利率</div>
            </div>
            <div class="slide-actions">
              <button class="btn-primary" onclick="showProductDetail(0)">查看詳情 →</button>
              <button class="btn-ghost" onclick="addToCart(0); showToast('Nova HE60 已加入購物車！')">加入購物車</button>
            </div>
          </div>
        </div>

        <!-- SLIDE 2 -->
        <div class="slide" data-idx="1">
          <div class="slide-visual">
            <div class="keyboard-display" style="background:linear-gradient(135deg,#0d1118,#111825)">
              <div class="keyboard-emoji">🎮</div>
              <div class="kb-label">Phantom 75HE · Midnight Purple</div>
            </div>
          </div>
          <div class="slide-info">
            <div class="slide-badge">⚡ 電競旗艦</div>
            <h1>Phantom<br><em>75HE</em></h1>
            <p>75% 完美配列，保留方向鍵的電競最佳選擇，
              CNC 陽極氧化鋁合金上蓋，質感全面進化。</p>
            <div class="slide-price-row">
              <div class="slide-price">NT$5,290</div>
              <div class="price-note">含稅・免運費<br>分期 0 利率</div>
            </div>
            <div class="slide-actions">
              <button class="btn-primary" onclick="showProductDetail(1)">查看詳情 →</button>
              <button class="btn-ghost" onclick="addToCart(1); showToast('Phantom 75HE 已加入購物車！')">加入購物車</button>
            </div>
          </div>
        </div>

        <!-- SLIDE 3 -->
        <div class="slide" data-idx="2">
          <div class="slide-visual">
            <div class="keyboard-display" style="background:linear-gradient(135deg,#0a1010,#0d1a18)">
              <div class="keyboard-emoji">🏆</div>
              <div class="kb-label">Titan HE100 · Space Gray</div>
            </div>
          </div>
          <div class="slide-info">
            <div class="slide-badge">👑 旗艦全尺寸</div>
            <h1>Titan<br><em>HE100</em></h1>
            <p>全尺寸 100% 配列旗艦款，數字鍵盤完整保留，
              重度使用者與直播主的終極選擇。</p>
            <div class="slide-price-row">
              <div class="slide-price">NT$5,990</div>
              <div class="price-note">含稅・免運費<br>分期 0 利率</div>
            </div>
            <div class="slide-actions">
              <button class="btn-primary" onclick="showProductDetail(2)">查看詳情 →</button>
              <button class="btn-ghost" onclick="addToCart(2); showToast('Titan HE100 已加入購物車！')">加入購物車</button>
            </div>
          </div>
        </div>

        <!-- SLIDE 4 -->
        <div class="slide" data-idx="3">
          <div class="slide-visual">
            <div class="keyboard-display" style="background:linear-gradient(135deg,#110d10,#1a1018)">
              <div class="keyboard-emoji">🌸</div>
              <div class="kb-label">Cloud Cream75 · Blush Pink</div>
            </div>
          </div>
          <div class="slide-info">
            <div class="slide-badge">✨ 限定款</div>
            <h1>Cloud<br><em>Cream75</em></h1>
            <p>奶油白限定配色，鵝毛觸感靜音軸，
              讓辦公桌也能充滿美感的高質感鍵盤。</p>
            <div class="slide-price-row">
              <div class="slide-price">NT$4,890</div>
              <div class="price-note">含稅・免運費<br>限量供應</div>
            </div>
            <div class="slide-actions">
              <button class="btn-primary" onclick="showProductDetail(3)">查看詳情 →</button>
              <button class="btn-ghost" onclick="addToCart(3); showToast('Cloud Cream75 已加入購物車！')">加入購物車</button>
            </div>
          </div>
        </div>

      </div>
    </div>

    <!-- Slide controls -->
    <div class="slide-controls">
      <button class="arrow-btn" onclick="prevSlide()">←</button>
      <button class="dot-btn active" onclick="goSlide(0)"></button>
      <button class="dot-btn" onclick="goSlide(1)"></button>
      <button class="dot-btn" onclick="goSlide(2)"></button>
      <button class="dot-btn" onclick="goSlide(3)"></button>
      <button class="arrow-btn" onclick="nextSlide()">→</button>
    </div>
  </section>

  <!-- PRODUCTS GRID -->
  <section class="products-section" id="products">
    <div class="section-header">
      <h2>全系列 <span>鍵盤</span></h2>
      <a class="see-all">查看全部 →</a>
    </div>

    <div class="products-grid" id="products-grid"></div>
  </section>

  <footer>
    <div class="footer-logo">Vertex<span>TW</span></div>
    <div class="footer-text">© 2026 VertexTW — Taiwan Premium Gaming Keyboards</div>
    <div class="footer-text">台灣製造・全球出貨</div>
  </footer>
</div>

<!-- ===== PRODUCT DETAIL PAGE ===== -->
<div class="page" id="page-detail">
  <div class="product-detail">
    <button class="back-link" onclick="showPage('home')">← 返回首頁</button>
    <div class="detail-grid">
      <div class="detail-visual" id="detail-visual">⌨️</div>
      <div class="detail-info">
        <div class="brand">VERTEXTW</div>
        <h1 id="detail-name">Nova HE60</h1>
        <div class="detail-badges" id="detail-badges"></div>
        <p class="detail-desc" id="detail-desc"></p>
        <div class="specs-grid" id="specs-grid"></div>
        <div class="detail-price-row">
          <div class="detail-price" id="detail-price">NT$4,590</div>
          <div class="detail-price-note">含稅 / 免運費<br>最多 24 期 0 利率</div>
        </div>
        <div class="color-section">
          <label>顏色選擇</label>
          <div class="color-options" id="color-options"></div>
        </div>
        <div class="switch-section">
          <label>軸體選擇</label>
          <div class="switch-options" id="switch-options"></div>
        </div>
        <div class="detail-actions">
          <button class="btn-buy-now" id="detail-buy-now">立即購買</button>
          <button class="btn-add-cart-large" id="detail-add-cart">加入購物車</button>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ===== CHECKOUT PAGE ===== -->
<div class="page" id="page-checkout">
  <div class="checkout-page">
    <button class="back-link" onclick="toggleCart()">← 返回購物車</button>
    <h1>結帳</h1>
    <p class="sub">填寫以下資訊以完成訂購</p>

    <!-- Steps -->
    <div class="steps">
      <div class="step active" id="step-1">
        <div class="step-num">1</div>
        <span>送貨資訊</span>
      </div>
      <div class="step-line" id="line-1"></div>
      <div class="step" id="step-2">
        <div class="step-num">2</div>
        <span>付款方式</span>
      </div>
      <div class="step-line" id="line-2"></div>
      <div class="step" id="step-3">
        <div class="step-num">3</div>
        <span>確認訂單</span>
      </div>
    </div>

    <div class="checkout-grid">
      <div class="checkout-form">

        <!-- Shipping info -->
        <div class="form-section">
          <h3><span class="icon">📦</span>送貨資訊</h3>
          <div class="form-row">
            <div class="form-group">
              <label>姓名</label>
              <input type="text" placeholder="王小明" id="f-name">
            </div>
            <div class="form-group">
              <label>電話</label>
              <input type="tel" placeholder="09XX-XXX-XXX" id="f-phone">
            </div>
          </div>
          <div class="form-row full">
            <div class="form-group">
              <label>電子郵件</label>
              <input type="email" placeholder="email@example.com" id="f-email">
            </div>
          </div>
          <div class="form-row full">
            <div class="form-group">
              <label>送貨地址</label>
              <input type="text" placeholder="台北市信義區信義路五段 7 號" id="f-addr">
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>城市</label>
              <input type="text" placeholder="台北市" id="f-city">
            </div>
            <div class="form-group">
              <label>郵遞區號</label>
              <input type="text" placeholder="110" id="f-zip">
            </div>
          </div>
        </div>

        <!-- Payment -->
        <div class="form-section">
          <h3><span class="icon">💳</span>付款方式</h3>
          <div class="payment-tabs">
            <div class="pay-tab active" onclick="selectPayTab(this, 'card')">
              <div class="pay-tab-icon">💳</div>信用卡
            </div>
            <div class="pay-tab" onclick="selectPayTab(this, 'transfer')">
              <div class="pay-tab-icon">🏦</div>銀行轉帳
            </div>
            <div class="pay-tab" onclick="selectPayTab(this, 'linepay')">
              <div class="pay-tab-icon">📱</div>Line Pay
            </div>
          </div>
          <div id="payment-fields">
            <div class="form-row full">
              <div class="form-group card-number-wrap">
                <label>信用卡號碼</label>
                <input type="text" placeholder="1234 5678 9012 3456" id="f-card" maxlength="19"
                  oninput="formatCard(this)">
                <span class="card-brand" id="card-brand">💳</span>
              </div>
            </div>
            <div class="form-row">
              <div class="form-group">
                <label>有效期限</label>
                <input type="text" placeholder="MM / YY" id="f-exp" maxlength="7"
                  oninput="formatExp(this)">
              </div>
              <div class="form-group">
                <label>安全碼 CVV</label>
                <input type="text" placeholder="123" maxlength="3" id="f-cvv">
              </div>
            </div>
            <div class="form-row full">
              <div class="form-group">
                <label>持卡人姓名</label>
                <input type="text" placeholder="WANG XIAO MING" id="f-cardname">
              </div>
            </div>
          </div>
        </div>

        <button class="btn-place-order" onclick="placeOrder()">確認下單 →</button>
      </div>

      <!-- Order Summary -->
      <div class="order-summary">
        <h3>訂單明細</h3>
        <div class="order-items" id="order-items"></div>
        <div class="order-totals">
          <div class="order-total-row"><span>小計</span><span id="order-subtotal">NT$0</span></div>
          <div class="order-total-row"><span>運費</span><span style="color:var(--green)">免費</span></div>
          <div class="order-total-row"><span>稅金 (含稅)</span><span>已含</span></div>
          <div class="order-total-row grand"><span>總計</span><span class="g-price" id="order-grand">NT$0</span></div>
        </div>
        <div class="secure-badges">
          <span class="secure-badge">🔒 SSL 加密</span>
          <span class="secure-badge">✅ 安全付款</span>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ===== SUCCESS PAGE ===== -->
<div class="page" id="page-success">
  <div class="success-page">
    <div class="success-content">
      <div class="success-icon">✓</div>
      <h1>訂單成立！</h1>
      <p>感謝您的購買！我們將在 24 小時內處理您的訂單，
        並發送確認郵件至您的信箱。</p>
      <div class="order-num" id="success-order-num">訂單編號：VTW-000000</div>
      <br>
      <button class="btn-primary" style="margin:0 auto" onclick="showPage('home')">繼續購物 →</button>
    </div>
  </div>
</div>

<script>
/* ===== DATA ===== */
const products = [
  {
    id: 0, emoji: '⌨️', name: 'Nova HE60', layout: '60%',
    badge: '60% · Hall Effect · Rapid Trigger',
    price: 4590,
    desc: '專為 FPS 競技玩家打造的 60% 緊湊配列鍵盤。搭載新一代霍爾磁軸技術，提供業界頂尖的 Rapid Trigger 可調觸發點功能，0.1mm 精度感應，讓每次操作都快人一步。',
    colors: ['#1a1a1a','#2d3561','#8b5a5a'],
    colorNames: ['Carbon Black','Midnight Blue','Deep Red'],
    switches: ['線性軸','觸感軸','靜音軸'],
    specs: [
      {l:'配列',v:'60%'},{l:'軸體',v:'Hall Effect 磁軸'},
      {l:'觸發點',v:'0.1–4.0mm 可調'},{l:'接口',v:'USB-C'},
      {l:'輪詢率',v:'1000Hz'},{l:'按鍵壽命',v:'1 億次'}
    ]
  },
  {
    id: 1, emoji: '🎮', name: 'Phantom 75HE', layout: '75%',
    badge: '75% · CNC 鋁合金 · Hall Effect',
    price: 5290,
    desc: '75% 完美配列，保留方向鍵與 F 列的電競首選。CNC 陽極氧化航太鋁合金上蓋，搭配 POM 定位板與多層矽膠墊體，打字聲學達到頂尖水準。',
    colors: ['#1c0f2b','#0f1e1c','#1a1512'],
    colorNames: ['Midnight Purple','Forest Green','Dark Bronze'],
    switches: ['線性軸','觸感軸'],
    specs: [
      {l:'配列',v:'75%'},{l:'外殼',v:'CNC 鋁合金'},
      {l:'定位板',v:'POM'},{l:'Gasket',v:'矽膠'},
      {l:'輪詢率',v:'8000Hz'},{l:'藍牙',v:'5.1 三模'}
    ]
  },
  {
    id: 2, emoji: '🏆', name: 'Titan HE100', layout: '100%',
    badge: '100% · 旗艦全尺寸 · 三模無線',
    price: 5990,
    desc: '全尺寸 100% 配列旗艦款，數字鍵盤完整保留，專為重度辦公與直播需求設計。重量級鋅合金配重底部，按鍵聲紮實低沉，每次輸入皆有儀式感。',
    colors: ['#202020','#1a1a20','#1a1510'],
    colorNames: ['Space Gray','Cosmic Black','Desert Sand'],
    switches: ['線性軸','觸感軸','靜音軸'],
    specs: [
      {l:'配列',v:'100%'},{l:'底部配重',v:'鋅合金'},
      {l:'無線',v:'2.4G + 藍牙'},{l:'電池',v:'8000mAh'},
      {l:'輪詢率',v:'1000Hz/8000Hz'},{l:'RGB',v:'南向 RGB'}
    ]
  },
  {
    id: 3, emoji: '🌸', name: 'Cloud Cream75', layout: '75%',
    badge: '75% · 靜音 · 奶油限定色',
    price: 4890,
    desc: '專為美感辦公桌打造的靜音版 75% 鍵盤。奶油白限定配色，搭配鵝毛觸感靜音磁軸，在安靜的辦公室或咖啡廳使用毫無壓力，打字體驗如絲般順滑。',
    colors: ['#f5ede0','#e8d5c0','#d4c4b0'],
    colorNames: ['Cream White','Ivory','Oat'],
    switches: ['靜音線性軸','靜音觸感軸'],
    specs: [
      {l:'配列',v:'75%'},{l:'聲學',v:'靜音 5dB'},
      {l:'外殼',v:'PC 透明上蓋'},{l:'重量',v:'1.2kg'},
      {l:'輪詢率',v:'1000Hz'},{l:'燈效',v:'南向 RGB'}
    ]
  },
  {
    id: 4, emoji: '⚡', name: 'Strike TKL Pro', layout: 'TKL',
    badge: 'TKL · 極速回報 · Pro 版',
    price: 4290,
    desc: '職業電競選手指定款 TKL 配列，極致精簡無數字鍵盤，留給滑鼠更大的移動空間。8000Hz 超高輪詢率，配合 Rapid Trigger，讓每一幀都不放棄。',
    colors: ['#0a0a0a','#12121a','#0a120a'],
    colorNames: ['Stealth Black','Navy','Forest'],
    switches: ['線性軸'],
    specs: [
      {l:'配列',v:'TKL (87鍵)'},{l:'輪詢率',v:'8000Hz'},
      {l:'觸發點',v:'0.1mm'},{l:'回報延遲',v:'< 0.5ms'},
      {l:'接口',v:'USB-C'},{l:'PCB',v:'熱插拔'}
    ]
  },
  {
    id: 5, emoji: '🎵', name: 'Mono Mini60', layout: '60%',
    badge: '60% · 聲學調校 · 手工潤軸',
    price: 3890,
    desc: '以聲學為優先的 60% 鍵盤，每個軸都經過手工潤滑與膜片處理。搭配純 PC 定位板與棉花消音，創造出渾厚低頻的「Thock」打字音，聲音玩家的心頭好。',
    colors: ['#1a1a1a','#2a2015','#151a1a'],
    colorNames: ['Matte Black','Warm Brown','Slate'],
    switches: ['手工潤線性軸','手工潤觸感軸'],
    specs: [
      {l:'配列',v:'60%'},{l:'定位板',v:'PC 透明'},
      {l:'消音',v:'棉花 + 矽膠'},{l:'潤軸',v:'手工潤 205g0'},
      {l:'聲學',v:'Thock 低頻'},{l:'PCB',v:'熱插拔'}
    ]
  }
];

/* ===== STATE ===== */
let cart = []; // [{productId, qty, switch, color}]
let currentSlide = 0;
let slideTimer;

/* ===== RENDER PRODUCTS GRID ===== */
function renderProductsGrid() {
  const grid = document.getElementById('products-grid');
  grid.innerHTML = products.map(p => `
    <div class="product-card" onclick="showProductDetail(${p.id})">
      <div class="product-card-img">${p.emoji}</div>
      <div class="product-card-body">
        <div class="badge">${p.badge}</div>
        <h3>${p.name}</h3>
        <p class="desc">${p.desc.substring(0,60)}…</p>
        <div class="product-card-footer">
          <div class="product-price">NT$${p.price.toLocaleString()}</div>
          <button class="add-cart-mini" onclick="event.stopPropagation(); addToCart(${p.id}); showToast('${p.name} 已加入購物車！')">加入購物車</button>
        </div>
      </div>
    </div>
  `).join('');
}

/* ===== SLIDESHOW ===== */
function goSlide(idx) {
  const slides = document.querySelectorAll('.slide');
  const dots = document.querySelectorAll('.dot-btn');
  slides[currentSlide].classList.remove('active');
  slides[currentSlide].classList.add('exit-left');
  setTimeout(() => slides[currentSlide].classList.remove('exit-left'), 600);
  currentSlide = idx;
  slides[currentSlide].classList.add('active');
  dots.forEach((d,i) => d.classList.toggle('active', i===idx));
  resetTimer();
}

function nextSlide() { goSlide((currentSlide+1) % 4); }
function prevSlide() { goSlide((currentSlide+3) % 4); }

function resetTimer() {
  clearInterval(slideTimer);
  slideTimer = setInterval(nextSlide, 5000);
}

/* ===== PAGES ===== */
function showPage(name) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById('page-' + name).classList.add('active');
  window.scrollTo(0,0);
}

function scrollToProducts() {
  showPage('home');
  setTimeout(() => {
    document.getElementById('products').scrollIntoView({behavior:'smooth'});
  }, 50);
}

/* ===== PRODUCT DETAIL ===== */
function showProductDetail(id) {
  const p = products[id];
  document.getElementById('detail-visual').innerHTML = `<span style="font-size:7rem;position:relative;z-index:1">${p.emoji}</span>`;
  document.getElementById('detail-name').textContent = p.name;
  document.getElementById('detail-price').textContent = `NT$${p.price.toLocaleString()}`;
  document.getElementById('detail-desc').textContent = p.desc;
  document.getElementById('detail-badges').innerHTML = p.badge.split('·').map(b =>
    `<span class="detail-badge">${b.trim()}</span>`
  ).join('');
  document.getElementById('specs-grid').innerHTML = p.specs.map(s =>
    `<div class="spec-item"><div class="spec-label">${s.l}</div><div class="spec-val">${s.v}</div></div>`
  ).join('');
  // Colors
  let selColor = 0;
  document.getElementById('color-options').innerHTML = p.colors.map((c,i) =>
    `<div class="color-swatch ${i===0?'selected':''}" style="background:${c}" title="${p.colorNames[i]}"
      onclick="selectColor(this)"></div>`
  ).join('');
  // Switches
  let selSwitch = 0;
  document.getElementById('switch-options').innerHTML = p.switches.map((s,i) =>
    `<button class="switch-opt ${i===0?'selected':''}" onclick="selectOpt(this,'switch')">${s}</button>`
  ).join('');

  document.getElementById('detail-buy-now').onclick = () => {
    addToCart(id); showToast(p.name + ' 已加入！'); goCheckout();
  };
  document.getElementById('detail-add-cart').onclick = () => {
    addToCart(id); showToast(p.name + ' 已加入購物車！');
  };

  showPage('detail');
}

function selectColor(el) {
  el.closest('.color-options').querySelectorAll('.color-swatch').forEach(s=>s.classList.remove('selected'));
  el.classList.add('selected');
}
function selectOpt(el, type) {
  el.closest('.'+type+'-options, .switch-options').querySelectorAll('button').forEach(b=>b.classList.remove('selected'));
  el.classList.add('selected');
}

/* ===== CART ===== */
function addToCart(productId) {
  const existing = cart.find(i => i.productId === productId);
  if (existing) { existing.qty++; }
  else { cart.push({productId, qty:1}); }
  updateCartUI();
}

function removeFromCart(productId) {
  cart = cart.filter(i => i.productId !== productId);
  updateCartUI();
}

function changeQty(productId, delta) {
  const item = cart.find(i => i.productId === productId);
  if (!item) return;
  item.qty += delta;
  if (item.qty <= 0) removeFromCart(productId);
  else updateCartUI();
}

function updateCartUI() {
  const total = cart.reduce((s,i) => s + i.qty, 0);
  document.getElementById('cart-count').textContent = total;

  const body = document.getElementById('cart-body');
  const summary = document.getElementById('cart-summary');
  const checkoutBtn = document.getElementById('btn-checkout');

  if (cart.length === 0) {
    body.innerHTML = `<div class="cart-empty"><div class="cart-empty-icon">🛍️</div><p>您的購物車是空的</p></div>`;
    summary.style.display = 'none';
    checkoutBtn.disabled = true;
    return;
  }

  body.innerHTML = cart.map(item => {
    const p = products[item.productId];
    return `<div class="cart-item-row">
      <div class="cart-item-img">${p.emoji}</div>
      <div class="cart-item-info">
        <div class="cart-item-name">${p.name}</div>
        <div class="cart-item-meta">${p.layout} · Hall Effect</div>
      </div>
      <div class="cart-item-right">
        <div class="qty-control">
          <button class="qty-btn" onclick="changeQty(${p.id},-1)">−</button>
          <span class="qty-num">${item.qty}</span>
          <button class="qty-btn" onclick="changeQty(${p.id},1)">+</button>
        </div>
        <div class="cart-item-price">NT$${(p.price*item.qty).toLocaleString()}</div>
        <button class="remove-item" onclick="removeFromCart(${p.id})">✕</button>
      </div>
    </div>`;
  }).join('');

  const subtotal = cart.reduce((s,i) => s + products[i.productId].price * i.qty, 0);
  document.getElementById('cart-subtotal').textContent = `NT$${subtotal.toLocaleString()}`;
  document.getElementById('cart-total-display').textContent = `NT$${subtotal.toLocaleString()}`;
  summary.style.display = 'block';
  checkoutBtn.disabled = false;
}

function toggleCart() {
  const overlay = document.getElementById('cart-overlay');
  const drawer = document.getElementById('cart-drawer');
  overlay.classList.toggle('open');
  drawer.classList.toggle('open');
}

/* ===== CHECKOUT ===== */
function goCheckout() {
  // close cart drawer
  document.getElementById('cart-overlay').classList.remove('open');
  document.getElementById('cart-drawer').classList.remove('open');
  // render order items
  const subtotal = cart.reduce((s,i) => s + products[i.productId].price * i.qty, 0);
  document.getElementById('order-items').innerHTML = cart.map(item => {
    const p = products[item.productId];
    return `<div class="order-item-row">
      <div class="order-item-emoji">${p.emoji}</div>
      <div class="order-item-info">
        <div class="order-item-name">${p.name}</div>
        <div class="order-item-qty">數量：${item.qty}</div>
      </div>
      <div class="order-item-price">NT$${(p.price*item.qty).toLocaleString()}</div>
    </div>`;
  }).join('');
  document.getElementById('order-subtotal').textContent = `NT$${subtotal.toLocaleString()}`;
  document.getElementById('order-grand').textContent = `NT$${subtotal.toLocaleString()}`;
  showPage('checkout');
}

function selectPayTab(el, type) {
  document.querySelectorAll('.pay-tab').forEach(t=>t.classList.remove('active'));
  el.classList.add('active');
  const fields = document.getElementById('payment-fields');
  if (type === 'card') {
    fields.innerHTML = `
      <div class="form-row full"><div class="form-group card-number-wrap">
        <label>信用卡號碼</label>
        <input type="text" placeholder="1234 5678 9012 3456" maxlength="19" oninput="formatCard(this)">
        <span class="card-brand" id="card-brand">💳</span>
      </div></div>
      <div class="form-row">
        <div class="form-group"><label>有效期限</label><input type="text" placeholder="MM / YY" maxlength="7" oninput="formatExp(this)"></div>
        <div class="form-group"><label>安全碼 CVV</label><input type="text" placeholder="123" maxlength="3"></div>
      </div>
      <div class="form-row full"><div class="form-group"><label>持卡人姓名</label><input type="text" placeholder="WANG XIAO MING"></div></div>`;
  } else if (type === 'transfer') {
    fields.innerHTML = `<div style="padding:20px;background:var(--bg);border-radius:12px;border:1px solid var(--border);color:var(--muted);font-size:0.9rem;line-height:1.8">
      銀行：台灣銀行 (004)<br>
      帳號：123-456-789-000<br>
      戶名：VertexTW Co., Ltd.<br><br>
      <span style="color:var(--green)">完成轉帳後請截圖並寄至 support@vertextw.com</span>
    </div>`;
  } else {
    fields.innerHTML = `<div style="text-align:center;padding:30px">
      <div style="font-size:3rem;margin-bottom:16px">📱</div>
      <p style="color:var(--muted)">點擊「確認下單」後將跳轉至 LINE Pay 付款頁面</p>
    </div>`;
  }
}

function formatCard(input) {
  let v = input.value.replace(/\D/g,'').substring(0,16);
  input.value = v.replace(/(.{4})/g,'$1 ').trim();
  const brand = document.getElementById('card-brand');
  if (brand) {
    if (v.startsWith('4')) brand.textContent = '💙';
    else if (v.startsWith('5')) brand.textContent = '🔴';
    else if (v.startsWith('3')) brand.textContent = '💚';
    else brand.textContent = '💳';
  }
}

function formatExp(input) {
  let v = input.value.replace(/\D/g,'');
  if (v.length >= 2) v = v.substring(0,2) + ' / ' + v.substring(2,4);
  input.value = v;
}

function placeOrder() {
  const orderNum = 'VTW-' + Math.random().toString(36).substring(2,8).toUpperCase();
  document.getElementById('success-order-num').textContent = '訂單編號：' + orderNum;
  cart = [];
  updateCartUI();
  showPage('success');
}

/* ===== TOAST ===== */
function showToast(msg) {
  const toast = document.getElementById('toast');
  document.getElementById('toast-msg').textContent = msg;
  toast.classList.add('show');
  setTimeout(() => toast.classList.remove('show'), 2800);
}

/* ===== INIT ===== */
renderProductsGrid();
resetTimer();
</script>
</body>
</html>
